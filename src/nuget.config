<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- Fuentes vacias: el proyecto no usa paquetes NuGet; el SDK se referencia como DLL local. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
